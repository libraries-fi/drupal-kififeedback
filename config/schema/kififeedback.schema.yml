# Schema for the configuration files of the Kififeedback module.
# TODO: Add notified email/user

kififeedback.channel.*:
  type: config_entity
  label: 'Feedback channel'
  mapping:
    id:
      type: string
      label: 'ID'
    name:
      type: label
      label: 'Name'
    url:
      type: label
      label: 'Url'
    status:
      type: boolean
      label: 'Status'
    description_normal:
      type: text_format
      label: 'Description'
    description_accessibility:
      type: text_format
      label: 'Description (accessibility)'
